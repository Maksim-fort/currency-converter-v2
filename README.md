Микросервис для конвертации валют с кэшированием в Redis
Предварительные требования
Go 1.25+

Docker

Docker Compose
Запуск через Docker (рекомендуется)

# 1. Клонируйте репозиторий
git clone <your-repo-url>
cd currency-converter-v2

# 2. Запустите все сервисы
docker-compose up -d

# 3. Откройте в браузере
#    Фронтенд: http://localhost:8080/ui/
#    API:      http://localhost:8080/api/v1/convert?from=USD&to=EUR&amount=100
#    Health:   http://localhost:8080/health
📡 API Endpoints
Конвертация валют

GET /api/v1/convert?from={from}&to={to}&amount={amount}

Пример ответа:

json
{
  "from": "USD",
  "to": "EUR",
  "amount": 100,
  "rate": 0.8526,
  "result": 85.26
}
Структура проекта

currency-converter-v2/
├── cmd/server/          # Точка входа приложения
├── internal/            # Внутренние пакеты
│   ├── app/            # Инициализация приложения
│   ├── config/         # Конфигурация
│   ├── handler/        # HTTP хендлеры
│   ├── service/        # Бизнес-логика
│   └── middleware/     # Middleware (CORS, логирование)
├── pkg/                # Общие пакеты
│   └── cache/          # Redis клиент
├── frontend/           # Веб-интерфейс
│   └── index.html      # HTML фронтенд
├── tests/              # Интеграционные тесты
├── Dockerfile          # Docker конфигурация
├── docker-compose.yml  # Docker Compose
├── go.mod              # Зависимости Go
├── Makefile            # Автоматизация команд
└── README.md           # Документация
Логирование
Приложение использует структурированное логирование через Zap:

json
{"level":"info","ts":1634567890,"msg":"Конвертация выполнена","from":"USD","to":"EUR","amount":100}